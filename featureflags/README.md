# featureflags

Small helpers for per-subject flag key lists (normalize / merge / contains).

Storage and HTTP stay in the product. Pair with `billing/entitlements` when flags
also unlock capabilities alongside plan features.
